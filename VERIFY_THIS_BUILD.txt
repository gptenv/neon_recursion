Neon Recursion WebGL/WASM V10.8

Expected app version marker:
  webgl-wasm-v10.8-2026-06-stripefix-reactive

Fixed/added:
- V10.8 layered reactive shader engine with underlay, warped camera layer, previous-frame displacement, overlay masks, and pseudo-alpha compositing.
- V10.8 shader tuning keeps saturated recursive contrast, brighter colour cycling, feedback displacement, and visible audio/video motion while keeping camera structure readable.
- Up/Down and vertical swipe-hold now control the randomizer seed offset (reseed baked/generated recipes live). -/= now control visual drive.
- Generated presets use a new 24-value schema for much broader audio/video/reactive variance.
- The 200 baked presets were regenerated and remapped through a mixed family/mode recipe permutation for stronger adjacent-slot variance.
- Recipes select distinct audio reaction lanes, motion lanes, shape transforms, mask reactions, and feedback personalities for stronger preset-to-preset behavioral variance.
- Recipes now also select composition, color, mask, and warp personalities so most presets do not rely on wobble-warping.
- -/= adjust visual drive (warp, colour, depth, feedback, reactions). Seed offset is now on arrows + vertical swipe/hold.
- New forged presets use browser entropy plus multiple candidate recipes biased toward recipe-lane distance from existing presets.
- F toggles browser fullscreen via the Fullscreen API.
- Pressing F always forces the HUD hidden; it never toggles HUD state.
- H remains an independent HUD toggle and can show the HUD while fullscreen is active.
- Fullscreen changes trigger a resize pass.
- Single click/tap forges the same generated preset that C forges, while swipes do not.
- Double-click, double-tap, and Space force fullscreen, hide the HUD, and toggle MP4/H.264 canvas recording/download.
- Recording object URLs are revoked after download initiation and on page unload.
- Recording copies each WebGL frame into a stable offscreen recording canvas and requests capture frames from that canvas to avoid Chrome/macOS fullscreen-canvas first-frame-only output.
- Recording start waits for fresh fullscreen-sized app frames after entering fullscreen before MediaRecorder is created.
- Mic audio and browser-shared system/tab audio are mixed for both audio reactivity and MP4 recording audio.
- Axis defaults remain: X=ON, Y=ON, Z=OFF.
- The bundled touchstone.wasm make hotfix remains.
- The WebGL sat3() shader hotfix remains.

Verification performed in sandbox:
- node --check app.js
- source ~/.profile && make clean && make
- verified app.js contains toggleFullscreen(), requestFullscreen(), exitFullscreen(), fullscreenchange
- verified F key path calls toggleFullscreen() and H key path uses setHUDVisible()
- verified Space and double-tap/click paths call toggleRecordingGesture()
- verified recording path combines offscreen manual canvas capture with cloned mixed-audio tracks
- verified recording start path calls settleDisplayForRecording() before startRecording()
- verified V10.8 fallback mode starts without shader compile errors
- verified Up/Down and vertical drag/hold change seed offset (and reseed recipes) and single click forges a new layered recipe
- verified vertical drag/hold changes seed without triggering forge; -/= adjust drive
- verified high-contrast V10-style layered shader compiles and forge/drive still work
- verified bounded flow/feedback tuning avoids the uniform noisy wash and keeps baked preset slots visually more distinct
- verified -/= adjust drive (no longer seed), Up/Down update the HUD seed offset + reseed the active recipe + clear fb, and C forges new entropy-backed presets without console errors
- V10.8: completely reworked 20 field families + 8 react/motion/shape/feedback lanes + core reactivity signals (body/air/beat/groove/detail + punchier slam/beat drive) for much more varied, musical, and visually interesting audio-reactive looks across all 200 baked presets.
- V10.8: introduced explicit stripeLane (recipe[17]) + stripeIntent gating; diagonal (x+y) stripes and stripe veils now only appear in presets with a deliberate "stripe personality" (rare, ~15% of baked) and further probabilistically/family-gated so accidental interlaced-looking diagonals are eliminated for the vast majority of presets.
- V10.8: refreshed baseRecipe permutation + low-biased stripe distribution + updated makeRecipe for forged presets; new VERSION and engine tags.
- V10.8: refreshed baked preset names and recipe mapping across 200 subject/behavior pairings, with explicit non-warp, color, mask, block, edge, feedback, radial, and warp personalities.
- node --check app.js still clean; distribution sim confirms striping now rare+intentional.
